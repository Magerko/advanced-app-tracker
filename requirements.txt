# Runtime dependencies for Advanced App Tracker.
PyQt6>=6.6
psutil>=5.9
pyqtgraph>=0.13
matplotlib>=3.7
numpy>=1.24

# Optional but recommended:
pynput>=1.7        # idle detection (mouse/keyboard activity)
bcrypt>=4.0        # strong hashing for the exit password
pygetwindow>=0.0.9 # cross-platform active-window fallback
